<%- include('header.ejs')%>
    <body>
        <main id="root">
            <h1>Quiz List</h1>
            <ul>
            <% for(var i = 0 ; i < message.length ; i++) { %>
                <li>
                    <a href="<%=message[i]%>"><%= message[i] %></a>
                </li>
            <% } %>
            </ul>
        </main>
        <%- include('footer.ejs')%>
    </body>
</html>