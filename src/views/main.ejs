<%- include('header.ejs')%>

</head>

<body>
    <main id="root">
        <img src="./public/images/icon.png" alt="Quiz Server Logo" />
        <h1>Quiz List</h1>
        <% for(var i = 0 ; i < message.length ; i++) { %>
        <p>
            <a tabIndex="0" href="<%=message[i]%>"><%= message[i] %></a>
        </p>
        <% } %>
    </main>
    <%- include('footer.ejs')%>
</body>

</html>
